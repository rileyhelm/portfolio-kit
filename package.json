{
  "name": "portfolio-cutdown-frontend",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run --configLoader runner",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jsdom": "^27.0.0",
    "typescript": "^5.8.3",
    "vitest": "^4.0.4"
  }
}

